// This file is used to include the ng-croppie library, 
//   by default it uses the minified version
//
// The line below brings in the minified version 

require('./minified/ng-croppie.min');

// Or, comment out the above line, and un-comment the one below:

// require('./unminified/ng-croppie');

module.exports = 'ngCroppie';
